-- This one makes me hungry :)

-- Notes:
-- 'return' stops the code and returns
-- the value without running the code that comes after.

-- sometimes that's annoying, sometimes it's useful!

print(1)
if z^2 < 2 then
	if y == -3 then
		return BROWN
	elseif y == 3 then
		return WHITE
	elseif x == -3 then
		return WHITE
	elseif y == 0 then
		return PEACH
	end
	
	return RED
end
