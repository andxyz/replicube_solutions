--[[
REMEMBER YOU ARE TRYING TO MATCH ----------->
Each time your code is run, 3 variables will be pre-set
for you, identifying the location of the current voxel.
These variables are 'x', 'y', and 'z' (without quotes)

You can use conditions to decide what value to return 
based on x, y, and z. ]]

if y > 0 then
	return DARKBLUE
else
	return ORANGE
end
-- 'end' doesn't mean 'end of the file'!
-- It means 'end of this IF THEN block'