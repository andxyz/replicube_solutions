-- Using the 3D View:
-- Look Around: click and drag left mouse button
-- Zoom In/Out: mouse wheel or click and drag right mouse button

-- remember, return 0 or 'EMPTY' to make empty space.
return (y > -3 and abs(x) ~= 3 and abs(z) ~= 3) and 0 or BROWN